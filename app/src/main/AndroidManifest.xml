<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hamara.gumasta">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HamaraGumasta"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.Authentications.OtpVerification">


        </activity>
        <activity android:name=".Activities.ThankYouScreen" />
        <activity android:name=".Activities.BusinessNature" />
        <activity android:name=".Activities.LicenseType" />
        <activity android:name=".Activities.SetLocation" />
        <activity android:name=".Activities.Authentications.CreateNewPass" />
        <activity android:name=".Activities.Authentications.ForgotPassword" />
        <activity android:name=".Activities.Authentications.Login_SignUp" />
        <activity android:name=".Activities.MainActivity" />
        <activity android:name=".Activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>